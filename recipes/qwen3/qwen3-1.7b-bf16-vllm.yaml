recipe_version: "2"
model: Qwen/Qwen3-1.7B
runtime: vllm
# Upstream multi-arch vLLM image (has an amd64 manifest). No `builder:` field, so
# sparkrun does a plain `docker pull` (no GB10 image build). Pin a tag with
# consumer-Blackwell (sm_120) support once validated on the target GPU.
container: rtx-vllm:latest   # entrypoint-less wrapper (see ../../images/) — required for sparkrun on amd64

metadata:
  description: Qwen3 1.7B (bf16) — amd64 + NVIDIA RTX, 32GB class (e.g. RTX 5090)
  maintainer: kreuzhofer
  model_params: 1.7B
  model_dtype: bf16
  category: chat

defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 1
  gpu_memory_utilization: 0.85
  max_model_len: 16384
  served_model_name: qwen3-1.7b

executor_config:
  devices: []                 # drop sparkrun's rootless --device /dev/infiniband (no InfiniBand on RTX hosts)
  restart_policy: unless-stopped

command: |
  vllm serve {model} \
      --served-model-name {served_model_name} \
      --host {host} \
      --port {port} \
      --tensor-parallel-size {tensor_parallel} \
      --gpu-memory-utilization {gpu_memory_utilization} \
      --max-model-len {max_model_len} \
      --trust-remote-code
